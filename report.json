{
  "4d0697d": "Confirmado: board autônomo não aprova pedidos (autoApply hardcoded para false). O pedido pendente fica órfão se, após pedir um status, o agente (ou o app via timeout) alterar o status diretamente via update_task. retainStatusAsk exige newActor === 'human' E proposedStatus !== null para limpar o pedido, então a escrita do agente aplica o novo status (limpando a divergência se for igual) mas o pedido continua existindo no BD/UI.",
  "1fcd36b": "Sim, existe byte que ainda reacende indevidamente. O xterm.js usa onData para emitir respostas automáticas do terminal (ex: Cursor Position Report, Device Status Report). Isso chama applyActivityRef.current(\"input\"), reabrindo o turno indevidamente (turnOpen=true) sem nenhuma tecla humana. O próximo byte acenderá a barra e a deixará presa.",
  "5e231a9": "Sim, existe turno real que não acende. O despejo de entrada humana adiada (deferred flush) usa proc.write direto no message-bus, sem emitir notifyCardInput. Como o turno anterior já emitiu turn_complete e fechou a janela (turnOpen=false), o eco do PTY chega como data e a barra fica apagada enquanto o shell processa essa entrada.",
  "a544d40": "Confirmado. system_prompt não é exposto por list_cards, nem lido por read_sticky. O updateStickyContentDirect altera apenas o cwd (content) via spread, preservando system_prompt. O parseStickyFontSize trata corretamente linhas antigas: Number(null/vazio) e lixo viram NaN, que cai no guard !Number.isFinite() na função clamp, retornando o default de 14."
}
