# agent-canvas

MVP: canvas de terminais reais (Electron + xterm.js + node-pty), com zoom
óptico real e cards livremente arrastáveis/redimensionáveis. Ver `AGENTS.md`
para contexto, decisões de design e riscos conhecidos.

```bash
npm install
npm run dev
```
